const  defaultState = {
    categoory: "",
    alpha:"",
    singerList:[],
    enterLoading:true,
    listOffset:0
}

export  default (state = defaultState,action)=>{
    switch(action.type){
        default:
            return
    }
}